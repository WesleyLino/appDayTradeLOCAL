import type { NextConfig } from "next";

// [ANTIVIBE-CODING] - Configuração base estável (Evitar chaves experimentais de Turbopack/V16 sem validação)
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
